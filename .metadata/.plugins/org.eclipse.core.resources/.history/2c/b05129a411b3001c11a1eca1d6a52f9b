/*
 ============================================================================
 Name        : Calculadoras Operaciones Básicas.c
 Author      : Alejandro Alberto Martín Rozas
 Version     : 0.1 del 01 de abril de 2022
 Copyright   :
 Objetive	 : Tomar numeros ingresados por el usuario
 	 	 	   y realizar operaciones matématicas simples.
 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>
#include <stdio_ext.h>
#include "Operaciones.h"
#include "Entrada.h"

#define TRUE  1
#define FALSE 0


int main(void)
{
	/*float numero1=20;
	float numero2=2;
	float resultado;
	int validacion;

	validacion = dividir(numero1, numero2, &resultado);
	if (validacion==0)
	{
		printf("El resultado es: %.2f \n", resultado);
	}
	else
	{
		printf("no se puede dividir por 0");
	}
	 */
	int opcion;
	int respuesta;

	do
	{
		menuUno("CALCULADORA EN C", "Operaciones básicas", "Otras operaciones", &opcion);
		switch(opcion)
		{
		case 1:
			menuTres("Operaciones básicas", "Sumar", "Restar", "Multiplicar", "Dividir", &opcion);
			break;
		default:
			menuUno("Otras operaciones", "Resto", "Producto", &opcion);
		}
	}while(respuesta==TRUE);


		prinf("FINALIZO EL PROGRAMA")
		return 0;
}


